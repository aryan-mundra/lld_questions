package enums;

/**
 * Severity levels, least to most severe. A logging system routes each message
 * to the handler responsible for its level.
 */
public enum LogLevel {
    DEBUG,
    INFO,
    WARN,
    ERROR
}
